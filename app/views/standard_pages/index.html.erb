<% @categories.each do |category| %>
	<div class="category">
		<%= category.name %>
		<div class="products-icon-container">
			<%= render partial:'products_icon', locals: {object: @products[category.name]} %>
		</div>
	</div>
<% end %>